# inviteMaster 项目审查与优化修改清单

> 审查日期：2026-08-19  
> 项目路径：`/Users/witung/Documents/ChatGPT/inviteMaster`  
> 当前分支：`main`  
> 当前提交：`b721459 v1.0.4`  
> 实施状态：已完成首轮 P0/P1 源码优化、依赖治理和稳定版微信开发者工具预览构建；未完成真实服务账号联调或真机验收。

## 零、本次直接优化结果

| 范围 | 状态 | 结果 |
| --- | --- | --- |
| 会话缓存 | 已完成 | 新增统一 session 模块；首页、接收页、个人资料页不再直接覆盖 `user` 缓存 |
| 请求与鉴权 | 已完成 | 请求所有失败路径均 reject；鉴权刷新 single-flight；限制重试次数；并发 loading 使用引用计数 |
| 依赖安全 | 已完成 | 删除未使用的 `we-cropper` 及 49 个传递依赖；`npm audit` 从 7 个漏洞降为 0 |
| 包体缓存 | 已完成 | 旧 `miniprogram_npm` 4.3 MB/88 文件已移至系统废纸篓，可恢复 |
| 生命周期 | 已完成 | Proxy 订阅返回注销函数，页面卸载和组件 detached 时释放 |
| 集合与空数据 | 已完成 | 修复 `splice(-1)`、收藏对象化、素材 map 删除、稳定 key 和首页空列表越界 |
| 音频文件 | 已完成首轮 | 等待保存完成、仅清理 `audio_` 前缀文件、等待删除完成并清理裁剪临时文件 |
| 创建页稳定性 | 已完成首轮 | 增加模板 JSON、样式更新、SelectorQuery 和快照失败保护；移除保存时图片预览调试行为 |
| HTTPS 资源 | 已完成源码修改 | 广告图片切换到严格 TLS 可访问的 `https://witknow.com/witInvite/ad2.jpg` |
| 工程门禁 | 已完成首轮 | 新增 TypeScript、优化范围严格类型检查、差异检查和 `npm run verify` |

本次验证结果：

- `npm ci`：通过，安装 2 个开发依赖。
- `npm run verify`：通过；覆盖本次优化高风险 TypeScript 路径与 `git diff --check`。
- `npm audit`：0 个已知漏洞。
- 稳定版微信开发者工具 `build-npm`：执行成功，并确认项目已无小程序生产 npm 包。
- 稳定版微信开发者工具 `preview`：通过，当前预览包体 `453.6 KB`（`464446` 字节）。
- 服务联调、iOS/Android 真机、公众平台合法域名：尚未验证。

## 一、审查结论

当前项目是原生微信小程序，使用 TypeScript、Sass、Skyline 和 glass-easel。现有功能已经具备邀请函模板浏览、编辑、分享、应邀、个人资料和素材管理等基本能力，但在登录缓存、网络请求、依赖安全、资源域名、全局状态订阅和媒体文件处理方面存在发布风险。

本轮已完成可在前端独立落地的主要 P0 问题和部分 P1 优化。正式发布前仍需完成真实账号服务联调、公众平台合法域名检查和 iOS/Android 真机回归。

## 二、P0：发布前必须修复

### 1. 修复登录缓存结构被覆盖

问题位置：

- `miniprogram/store/global.ts:67`
- `miniprogram/pages/mine/info/info.ts:150`
- `miniprogram/pages/index/index.ts:150`
- `miniprogram/pages/receive/receive.ts:35`

当前行为：

- 登录成功后，`user` 缓存结构为 `{ token, openid, userinfo, perlogo }`。
- 修改个人资料后，同一个 `user` key 被覆盖成单独的 `JSON.stringify(userInfo)`。
- 下次进入小程序时，首页和分享接收页仍按原缓存结构读取，最终得到空 token/openid。

修改建议：

- [x] 新建统一的 session storage 模块，禁止页面直接读写 `user` key。
- [x] 保存个人资料时只更新缓存中的 `userinfo` 字段，保留 token、openid 和 perlogo。
- [x] 对缓存内容增加运行时结构校验。
- [x] 兼容已经写入字符串或旧结构的缓存；识别失败时重新登录并覆盖旧缓存。
- [x] 统一异步 `wx.getStorage` 与 `wx.setStorage` 的错误处理。

验收标准：

- [ ] 登录后修改头像、昵称、手机号、性别和生日均可正常保存。
- [ ] 修改资料后杀掉小程序进程并重新进入，仍保持有效登录态。
- [ ] 旧缓存、空缓存、损坏缓存都不会导致页面卡死。
- [ ] 首页、我的、应邀和分享接收页使用同一套缓存读取逻辑。

### 2. 重构 `_request`，保证所有 Promise 都能结束

问题位置：`miniprogram/utils/util.ts:75`

当前风险：

- 网络失败且 `throwCatch=false` 时只显示 Toast，没有 resolve/reject，Promise 会一直 pending。
- 鉴权重试次数耗尽后没有 reject。
- `header` 是可选参数，但登录刷新后直接执行 `header.token = token`。
- 多个请求同时收到鉴权失败时，可能并发调用 `wx.login`。
- `fail` 中存在 `return` 后不可达代码。
- 每个请求都直接调用全局 `wx.hideLoading`，并发请求之间可能互相关闭加载状态。

修改建议：

- [x] `_request<T>` 支持泛型响应，新增结构化 `RequestError`。
- [x] 所有成功、HTTP 失败、超时、刷新失败和重试耗尽分支都必须 resolve 或 reject。
- [x] 为 header 提供默认空对象，不直接修改调用者传入的对象。
- [x] 把 token 刷新封装成 single-flight Promise，同一时间只允许一次登录刷新。
- [x] 明确最大重试次数，耗尽后返回可识别的鉴权错误。
- [x] 将加载状态改为引用计数，避免并发请求互相干扰。
- [ ] 日志只输出脱敏后的 URL、状态码和 requestId。

至少覆盖以下测试：

- [ ] 正常响应。
- [ ] 业务失败响应。
- [ ] HTTP 非 2xx。
- [ ] 网络断开和超时。
- [ ] 第一次鉴权失败、刷新成功、重试成功。
- [ ] 刷新失败。
- [ ] 连续鉴权失败直到重试耗尽。
- [ ] 多个请求同时鉴权失败时只触发一次登录刷新。

### 3. 删除未使用的 `we-cropper` 依赖

问题位置：

- `package.json:13`
- `package-lock.json`
- `project.config.json:20`

审查结果：

- 源码中没有发现 `we-cropper` 的 import、require 或组件引用。
- 该依赖带入 49 个生产传递依赖。
- 当前 `miniprogram/miniprogram_npm` 实测约 4.3 MB、88 个生成文件。
- 优化前 `npm audit` 报告 7 个漏洞：3 critical、2 high、2 low；本轮完成后为 0。

修改建议：

- [x] 使用 `npm uninstall we-cropper` 删除未使用依赖并更新 lockfile。
- [x] 将旧的 `miniprogram/miniprogram_npm` 移至废纸篓，并重新执行微信开发者工具 npm 构建。
- [x] 重新执行 `npm audit`，结果为 0 个已知漏洞。
- [ ] 在开发者工具上传面板中记录真实主包大小，不能只依据磁盘目录大小判断。
- [ ] 不执行可能强制降级依赖的 `npm audit fix --force`。

验收标准：

- [x] 源码功能不依赖 `we-cropper`。
- [x] npm 构建命令成功，并确认无生产 npm 包需要构建。
- [x] 旧生成目录已移出项目，不再携带无关包。
- [x] 稳定版开发者工具预览包体为 453.6 KB。

### 4. 修复 HTTP 广告资源和 HTTPS 证书

问题位置：`miniprogram/pages/invitation/invitation.wxml:44`

当前地址：`http://witknow.net/witInvite/ad2.jpg`

实测结果：

- HTTP 地址能返回图片。
- 对应 HTTPS 地址存在证书与域名不匹配问题。
- 真机和正式发布环境可能拒绝加载该资源。

修改建议：

- [ ] 为 `witknow.net` 配置正确、可信且未过期的 HTTPS 证书。
- [x] 将图片切换到项目已有且严格 TLS 可访问的 `witknow.com` HTTPS 地址。
- [ ] 在微信公众平台配置合法下载域名。
- [ ] 图片建议压缩并转换为 WebP，在保证清晰度的前提下降低当前约 209 KB 的体积。

验收标准：

- [ ] 严格 TLS 校验下可以访问图片。
- [ ] 开启开发者工具“不校验合法域名”关闭状态后仍可加载。
- [ ] iOS、Android 真机均显示正常。

## 三、P1：重要稳定性和性能优化

### 5. 为全局 Proxy 订阅增加注销机制

问题位置：`miniprogram/app.ts:65`

当前 `setProxy` 只会向数组追加监听回调，没有移除接口。首页、我的、应邀、素材组件、创建页和 TabBar 反复加载后，旧页面实例会继续被回调引用。

修改建议：

- [x] 将 `setProxy(key, observer)` 改为返回 `unsubscribe()`。
- [x] 页面在 `onUnload`、组件在 `detached` 中注销订阅。
- [x] 删除只记录日志、不更新 UI 的订阅。
- [ ] 评估是否可以用页面主动查询和明确事件替代全局 Proxy。

复杂度目标：

- 当前：全局共享状态与页面生命周期隐式耦合，更新成本随历史页面访问次数增长。
- 优化后：每个页面只拥有当前生命周期内的有限订阅，更新复杂度与当前活跃消费者数量相关。

验收标准：

- [ ] 连续进入和退出同一页面 20 次，订阅数量不增长。
- [ ] 已卸载页面不再收到 `setData`。
- [ ] 全局状态更新只触发当前可见或仍存活的消费者。

### 6. 修复音频保存竞态和清理范围

问题位置：

- `miniprogram/components/material/audioCut/audioCut.ts:70`
- `miniprogram/utils/wxapp/clean.ts:33`

修改建议：

- [x] `_loadAudio` 改为等待 `saveFileWithCleanup` 完成，并使用实际返回的 savedPath。
- [x] 捕获保存 Promise rejection，避免未处理异常。
- [x] 只删除音频裁剪功能创建且带固定前缀的临时文件。
- [x] 删除文件时等待操作完成，不使用固定 300ms 延迟猜测结果。
- [x] 用户取消、组件销毁、上传完成后及时清理临时文件。
- [ ] 降低 50 MB 输入上限，或先根据时长、编码和预计解码内存进行限制。
- [ ] 对大文件、双声道、高采样率和低内存设备做真机压测。

### 7. 拆分 662 行邀请函创建页

问题位置：`miniprogram/pages/show/create/create.ts`

当前页面同时负责：

- 模板 JSON 解析和 CSS 序列化。
- 邀请函草稿与表单同步。
- 农历和日期处理。
- 字体、图片和音乐编辑。
- 定位选择。
- 快照生成和封面上传。
- 邀请函保存。
- 音频播放生命周期。

建议拆分：

- [ ] `templateCodec.ts`：模板解析、样式转换和序列化。
- [ ] `invitationDraft.ts`：草稿初始化、字段同步和校验。
- [ ] `snapshotUploader.ts`：快照生成、临时文件、上传和清理。
- [ ] `audioController.ts`：播放、暂停、切换和销毁。
- [ ] 页面层只保留事件编排与 `setData`。

同步修复：

- [x] `JSON.parse` 增加异常处理和数组结构校验。
- [x] SelectorQuery 结果为空时返回明确错误。
- [x] `takeSnapshot.fail` 使用箭头函数，确保 `this` 指向 Page 实例。
- [x] 样式属性不存在时追加属性，不再执行 `splice(-1, 1, ...)`。
- [x] 序列化时忽略空样式项，避免生成空字符串字段。
- [x] 删除保存流程中的 `wx.previewImage` 调试行为。

复杂度目标：

- 当前：单文件 662 行，多类副作用交织，错误路径难以覆盖。
- 优化后：页面模块主要负责流程编排，纯数据模块可以独立单测；单个函数尽量控制在 50 行以内。

### 8. 修复集合类型和边界值

问题位置：

- `miniprogram/pages/mine/mine.ts:106`
- `miniprogram/pages/mine/mine.wxml:62`
- `miniprogram/store/favorite.ts:23`
- `miniprogram/store/response.ts:42`
- `miniprogram/store/invitation.ts:57`
- `miniprogram/store/material.ts:60`

修改建议：

- [x] 收藏列表保持数组：`favorites: [...value]`，不要转换成对象。
- [x] `favorites%2` 改为 `favorites.length % 2`。
- [x] 所有删除逻辑在 `index >= 0` 时才能执行 `splice`。
- [x] `removeMaterial` 按 material map 的真实结构定位分类和列表，不把 map 当数组。
- [x] 所有本轮涉及的列表 WXML 使用稳定 `wx:key="id"`。
- [ ] 删除空的 `key=""`、`wx:key=""` 和空事件绑定。

### 9. 为首页增加空数据和异常状态

问题位置：`miniprogram/pages/index/index.ts:24`

修改建议：

- [x] 模板分类为空时禁止访问 `types[0].id`，保持空列表。
- [x] 字体列表为空时跳过加载，不影响模板浏览。
- [x] 分类接口和字体接口失败时分别降级，避免一个失败阻断整个首页。
- [ ] 对页面卸载后的异步回调增加存活判断。
- [ ] 首页首次加载所需请求可并发执行，并统一呈现加载状态。

### 10. 清理敏感日志

问题位置：

- `miniprogram/utils/util.ts:97`
- `miniprogram/store/global.ts:61`
- 其他页面和组件中的大量 `console.log`

当前请求日志可能包含 token、openid、手机号、个人资料和接口响应。

修改建议：

- [ ] 新建统一 logger，区分开发和生产环境。
- [ ] token、openid、手机号等字段必须脱敏。
- [ ] 生产环境关闭调试日志，只保留必要的错误码和 requestId。
- [ ] 禁止打印完整登录响应、请求 header 和表单参数。

## 四、P2：维护性和产品安全优化

### 11. 收紧分享访问模型

当前分享地址直接使用邀请函数字 ID：`/pages/receive/receive?code=${invitation.id}`。

- [ ] 改为服务端签发的不可猜测 shareToken。
- [ ] 服务端校验邀请函已发布、未取消、未过期且允许访问。
- [ ] 防止通过递增 ID 枚举其他邀请函。
- [ ] 分享 token 支持失效、轮换和访问统计。

> 该项必须由服务端共同验证；仅修改前端不能防止 IDOR。

### 12. 限制 sitemap 收录范围

问题位置：`miniprogram/sitemap.json`

- [ ] 只允许公开首页或明确需要搜索收录的页面。
- [ ] 禁止收录个人资料、我的、邀请函编辑、素材管理等私有页面。

### 13. 将日期和农历逻辑改为纯函数

问题位置：

- `miniprogram/utils/format/index.js`
- `miniprogram/utils/format/lunarDate.js`

修改建议：

- [ ] 停止扩展 `Date.prototype` 和 `String.prototype`。
- [ ] 提供 `formatDate`、`parseDate`、`formatLunarDate` 等显式纯函数。
- [x] 修复农历计算中反复 `monStrs.splice` 导致全局月份数组持续变化的问题。
- [ ] 删除小程序不需要的 DOM、`document` 和浏览器事件代码。
- [ ] 补充闰月、跨年、非法日期和边界年份测试。

### 14. 建立工程质量门禁

当前 `package.json` 没有任何 scripts，也没有测试、README 和 CI 配置。

建议增加：

```json
{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint miniprogram --ext .ts,.js",
    "test": "vitest run",
    "check:diff": "git diff --check",
    "verify": "npm run typecheck && npm run lint && npm run test && npm run check:diff"
  }
}
```

实施要求：

- [ ] 将 TypeScript、ESLint 和测试框架加入 devDependencies。
- [ ] 逐步减少 `any`，API 数据先使用 `unknown` 再通过类型守卫解析。
- [ ] 开启 `noUncheckedIndexedAccess` 前先修复数组越界问题。
- [ ] 为 session、request、模板序列化、农历和集合 reducer 增加单元测试。
- [ ] CI 执行 `npm ci`、`npm run verify` 和依赖审计。
- [ ] 微信开发者工具或 `miniprogram-ci` 构建作为独立交付门禁。

## 五、推荐实施顺序

### 第一阶段：稳定性修复

- [x] 修复 session 缓存结构。
- [x] 重构 `_request` 和鉴权刷新。
- [x] 修复收藏列表、数组删除和空数据崩溃。
- [x] 修复 HTTPS 广告资源。

### 第二阶段：依赖和包体治理

- [x] 删除 `we-cropper`。
- [x] 重新安装依赖并构建 npm。
- [x] 重新执行安全审计。
- [x] 通过稳定版开发者工具预览记录当前包体大小。

### 第三阶段：生命周期和媒体链路

- [x] 为 Proxy 订阅增加注销。
- [x] 修复音频保存竞态和临时文件清理。
- [ ] 完成大音频真机内存测试。

### 第四阶段：架构拆分和工程化

- [ ] 拆分创建页。
- [ ] 日期与农历逻辑纯函数化。
- [ ] 已增加优化范围 typecheck 和 diff gate；lint、单元测试与 CI 待补充。
- [ ] 完善 README、开发说明和发布检查表。

## 六、最终验收矩阵

| 验收层级 | 必须完成的检查 |
| --- | --- |
| 源码检查 | 本轮优化范围 typecheck 与 `git diff --check` 已通过；lint、单元测试待补充 |
| 依赖检查 | `npm ci` 成功，生产依赖无已知高危/严重漏洞 |
| 开发者工具 | 稳定版 npm 构建命令和预览编译已成功；无生产 npm 包，预览包体 453.6 KB |
| 服务联调 | 登录刷新、资料保存、模板查询、邀请函保存、收藏、应邀、素材上传均使用真实账号验证 |
| 异常场景 | 断网、超时、接口 400、刷新失败、空列表、损坏缓存、大文件和存储空间不足均有明确结果 |
| 真机验证 | iOS、Android 至少各一台，验证 Skyline、字体、定位、音频、快照、分享和 HTTPS 资源 |
| 发布检查 | 主包大小、合法域名、隐私声明、sitemap、版本更新和体验版回归全部确认 |

## 七、证据边界

- 已确认：源码结构、代码问题、依赖树、生成 npm 目录大小、当前安全审计结果、HTTP/HTTPS 资源状态。
- 已通过：`npm ci`、本轮优化范围严格 TypeScript 检查、`git diff --check`、`npm audit`、稳定版开发者工具预览编译。
- 已确认：当前预览包体 453.6 KB；旧 npm 生成目录已移至系统废纸篓。
- 未确认：私有接口是否可用、后端是否防止越权访问。
- 未确认：体验版、正式版和真实设备表现。

完成修改后，应分别报告“源码通过”“开发者工具通过”“服务联调通过”和“真机通过”，不要用单一构建结果替代完整验收。
