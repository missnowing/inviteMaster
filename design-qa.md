# inviteMaster UI 视觉校验

## Comparison Target

- 视觉母版：`docs/design/inviteMaster-ui-reference-v4-layout.png`
- 母版尺寸：`1702 x 924`，包含首页、我的应邀、创作中心、编辑邀请函四屏。
- 实现截图：`docs/design/qa/home.png`、`invitation.png`、`mine.png`、`editor.png`。
- 并排对比：`docs/design/qa/reference-vs-implementation.png`；上排为母版，下排为微信开发者工具实装截图。
- 实现视口：微信开发者工具 `iPhone 12/13 (Pro)` Skyline 模拟器；统一裁切为 `286 x 623`。
- 校验时间：2026-08-20。

## Findings

- 未发现阻止交付的 P0/P1 视觉问题。
- 首页已对齐母版的宣纸底色、左对齐导航标题、搜索/分类节奏、精选横卡和三列新作比例；真实模板封面来自当前接口，因此题材与母版示例不同。
- 我的应邀在当前账号没有记录，实装截图验证的是山水竹枝空状态；有数据时首条使用折角纸卡、山水、竹枝和朱砂印章，其余记录进入时间轴。
- 创作中心已对齐头像、四项统计、竹枝新建入口、折角收藏/素材卡、作品状态筛选和底部导航顺序。
- 编辑器已验证空模板回退画面，窗棂、山水、栏杆、祥云和朱砂印章均在微信模拟器实际渲染；正常业务入口仍优先显示接口模板，不会把装饰强行写入用户作品。

## Fidelity Surfaces

- 字体与层级：标题使用宋体系 fallback，正文使用系统中文字体；标题、统计、辅助文案层级清楚。
- 间距与布局：四屏均保留状态栏、胶囊按钮、底部安全区；主卡片、三列封面、快捷卡和编辑器固定操作区没有可见溢出。
- 色彩与令牌：宣纸、松石绿、朱砂、灰墨、赭金与母版一致；状态标签保留业务语义色。
- 图像与素材：页面引用透明 PNG，原 SVG 作为可编辑源文件；微信 Skyline 已验证透明通道和缩放效果。
- 交互与业务：TabBar 顺序恢复为“首页 / 应邀 / 我的”；筛选、快捷入口、编辑器原有事件和保存发布路径保留。

## Verification Evidence

- `npm run typecheck`：通过。
- `npm run check:diff`：通过。
- 微信开发者工具最终编译：首页显示 `Errors: 0`；首页、应邀、创作中心和编辑器均已取得真实模拟器截图。
- 视觉对比板：`docs/design/qa/reference-vs-implementation.png`。

## Remaining Acceptance Boundary

- 当前本地账号没有应邀记录和作品数据，因此“最近一场”纸卡及作品列表的动态内容密度还需在有真实数据的账号上补一次真机验收。
- 微信开发者工具仍报告项目既有的 Skyline/旧 API 警告；本轮没有扩大到全项目兼容性重构。

final result: pass

## 2026-08-20 插图精修增量

- 新增 `miniprogram/assets/illustrations/courtyard-gathering-v2.jpg`，采用松石绿、朱砂、赭金和灰墨的工笔水墨庭院构图，左上为文案安全区，右侧与底部承载视觉重心。
- 已应用到首页搜索空状态、应邀首卡/空状态和创作中心空作品状态；不覆盖真实模板封面，也不写入用户编辑画布。
- 素材从 1536 × 1024 生成稿压缩为 1080 × 720、约 205 KB 的小程序本地 JPEG；`npm run verify` 通过。
- 本文上方的微信开发者工具截图早于本次插图精修，仍可作为原 UI 基线，但不能替代本次改动后的模拟器/真机视觉验收。

## 2026-08-20 预览页收敛改版

- 视觉母版：`/var/folders/09/rq8qpj914pb05b4qh7dvj69c0000gn/T/codex-clipboard-6438445f-5783-4e3b-bf92-655d4b428aee.png`。
- 母版像素尺寸：`410 x 880`；状态为已选模板预览。
- 目标视口：微信开发者工具手机模拟器，页面内容跟随设备宽度；本轮未取得改版后的模拟器截图，因此无法记录实现像素尺寸与密度归一化结果。
- 实现范围：邀请函成品图占据导航栏与底部操作区之间的全部空间；就绪态仅显示“收藏模板 / 制作模板”两项操作。

### Fidelity Surfaces

- 字体与层级：代码沿用项目的宋体标题与中文正文栈；缺少改版后截图，字号和字重仍待视觉确认。
- 间距与布局：代码已移除模板说明区并扩展预览舞台；缺少同视口截图，底部安全区、作品裁切和纵向节奏仍待视觉确认。
- 色彩与令牌：操作区沿用项目既有宣纸、松石绿与朱砂令牌，无新增色系。
- 图像与素材：继续使用接口返回的真实邀请函图片与 `widthFix`，未添加占位图、CSS 绘图或新装饰素材。
- 文案与功能：就绪态仅保留“收藏模板 / 取消收藏”和“制作模板”；收藏对象统一为模板，制作仍进入原模板编辑流程。

### Comparison Evidence

- 全屏对比：已打开母版；改版后实现截图缺失，无法形成同视口并排对比。
- 聚焦区域对比：底部双按钮与邀请函裁切同样因实现截图缺失而未执行。
- 代码验证：`npm run verify` 通过，含 TypeScript 检查、4 项模板编解码测试及 `git diff --check`。

### Findings

- [P2] 缺少改版后的微信模拟器截图，暂时不能确认邀请函首屏裁切、按钮安全区和 410 x 880 视口下的实际比例。
- 交互链路已在代码层确认：收藏调用模板收藏接口，制作携带 `templateId` 并继续通过原事件通道传递模板。

final result: blocked

## 2026-08-20 预览页视觉收敛第二轮

- 问题证据：`/var/folders/09/rq8qpj914pb05b4qh7dvj69c0000gn/T/codex-clipboard-b3d48ac2-a637-4e16-a24d-400e7173087b.png`。
- 证据尺寸：`416 x 877`；微信开发者工具 Skyline 模拟器中的模板预览就绪态。
- 用户目标：弱化底部操作，让邀请函成为唯一视觉主体。
- 改版后实现截图：缺失。稳定版开发者工具已重新打开并显示 `Errors: 0`，但当前模拟器停留在首页，未取得同模板、同视口的新版预览截图。

### Pre-fix Findings

- [P1] 全宽白色操作容器与邀请函形成两个并列视觉主体，朱砂主按钮面积过大。
- [P2] 两个按钮等宽且同为大胶囊，收藏与制作的主次关系不清。
- [P2] 图片结束后预览舞台仍保留带边框的纸色空白，容易被理解为邀请函未铺满或裁切异常。

### Fixes Applied

- 预览横向内边距从 `24rpx` 收紧到 `12rpx`，作品最大宽度从 `702rpx` 增加到 `726rpx`。
- 边框和阴影从满高预览舞台移到真实邀请函元素，图片结束后的空间不再表现为一块空画框。
- 删除全宽操作栏的背景、边框和阴影；操作组改为右下角题签式布局。
- 收藏降为透明次操作，制作按钮缩减到 `218rpx`；两者保留 `84rpx` 点击高度、按压反馈和无障碍标签。

### Verification

- `npm run verify`：通过，包含 TypeScript 检查、4 项模板编解码测试和 `git diff --check`。
- 微信开发者工具：稳定版重新打开；当前首页调试器显示 `Errors: 0`，项目既有 `Warnings: 15`。
- 视觉对比：因新版目标页截图缺失，无法完成同视口并排对比；邀请函占比、底部安全区和按钮实际观感仍需目标页截图确认。

final result: blocked

## 2026-08-20 预览页方案 1 定稿落地

- 视觉母版：`/Users/witung/.codex/generated_images/01a01de0-b238-7232-b4f6-bd594413cc4e/exec-1f8b7b4f-8a53-4b1b-ac3d-f4a94782ad93.png`。
- 用户确认：采用方案 1；操作改为按钮形状，文案为“收藏 / 制作”，收藏使用爱心图标。
- 实现范围：仅调整 `miniprogram/pages/show/show.wxml` 与 `show.scss`，保留真实预览图、模板收藏接口与制作跳转。

### Fixes Applied

- 预览区域延伸到底部安全区上方，操作组改为定位在邀请函右下角，不再预留独立底部工具栏。
- 收藏与制作改为 `164rpx / 152rpx` 的紧凑圆角矩形按钮，保持 `84rpx` 点击高度。
- 收藏按钮复用项目 iconfont 的 `icon-xihuan1 / icon-xihuan` 空心与实心爱心；收藏状态只改变图标与强调色，显示文案始终为“收藏”。
- 制作按钮使用既有朱砂色令牌，文案缩短为“制作”；原点击事件、模板参数和收藏状态逻辑均未改动。

### Verification

- `npm run verify`：通过，包含 TypeScript 检查、4 项模板编解码测试与全仓 `git diff --check`。
- 静态最小用例：确认“收藏 / 制作”文案、爱心状态图标、圆角按钮样式与预览内定位均存在，旧长文案已从 WXML 移除。
- 本轮遵循本地代码验证边界，未自动操作微信开发者工具取得同视口截图；按钮对动态邀请函内容的遮挡情况与真机安全区仍需手动视觉确认。

final result: blocked

## 2026-08-20 制作邀请函方案 1 定稿落地

- 视觉母版：`docs/design/inviteMaster-create-ui-reference-v2-text-tabs.png`。
- 母版尺寸：`853 x 1844`；目标状态为内容模块下的活动日期编辑。
- 实装截图：`docs/design/qa/create-confirmed.png`，尺寸 `286 x 623`。
- 开发者工具全屏证据：`docs/design/qa/create-confirmed-full.png`。
- 同视口并排对比：`docs/design/qa/create-reference-vs-implementation.png`；左侧为母版，右侧为微信开发者工具实装。
- 实现视口：微信开发者工具 Stable v2.01.2510290，`iPhone 12/13 (Pro)`，Skyline，100%。

### Fidelity Surfaces

- 导航：标题保持居中，右上“待保存 / 已同步”状态已删除；微信原生状态栏、返回能力和胶囊按钮按运行时规范保留。
- 模块：内容、样式、图片、音乐、设置统一为纯文字标签，无模块图标；当前项使用松石绿文字与底部短线。
- 内容区：默认显示活动日期，保留日期选择与“显示农历”；主题、时间、地点、邀请词和邀请人通过“切换字段”进入。
- 操作区：保存草稿为松石绿描边次按钮，发布为朱砂主按钮；两者保持 88rpx 点击高度。
- 画布：继续渲染接口返回的真实动态模板；覆盖文字和图片改为相对实际画布宽度缩放，不再受页面 `vw` 与画布内边距差异影响。
- 状态保护：重复保存有禁用态；未保存离开启用系统确认；草稿保存后停留当前页，主动发布后才返回。

### Comparison Findings

- 未发现可执行的 P0、P1 或 P2 视觉问题。
- 母版中的婚礼图片与实装中的升学宴属于动态模板内容差异，不是布局偏差；两者的画布主次、模块导航、日期编辑和双按钮层级一致。
- 实装顶部比母版多出微信状态栏和胶囊区域，属于移动运行时必要差异。
- 五个文字模块均已逐项点击验证，面板截图哈希各不相同，说明切换事件和对应内容区域实际生效。

### Verification

- `npm run verify`：通过，包含 TypeScript 检查、5 项模板编解码/缩放测试和 `git diff --check`。
- 微信开发者工具：目标路由 `pages/show/create/create?templateId=35` 实际渲染；直达路由缺少事件通道的问题已兼容回退到查询参数。
- 模块交互后控制台无应用错误；唯一出现过的 `reportRealtimeAction:fail not support` 为开发者工具 worker 警告，清理后等待 2 秒保持 `Errors: 0 / Warnings: 0`。
- 本轮未调用保存或发布接口，避免改动线上邀请函数据；未执行真机验收。

final result: passed

## 2026-08-20 制作页底部安全距离收紧

- 问题截图：`/var/folders/09/rq8qpj914pb05b4qh7dvj69c0000gn/T/codex-clipboard-b5c8c73d-e4a2-4e7f-91cd-9b566e38d207.png`，`451 x 446`，聚焦底部操作区。
- 实装截图：`docs/design/qa/create-safe-area-tightened.png`，`286 x 623`；聚焦截图为 `docs/design/qa/create-safe-area-tightened-focus.png`，`286 x 190`。
- 同视口前后对比：`docs/design/qa/create-safe-area-before-vs-after.png`，左右两屏均归一化为 `286 x 623`。
- 视口：微信开发者工具 Stable v2.01.2510290，`iPhone 12/13 (Pro)`，Skyline，100%。

### Findings and Fix

- [P2 已修复] 原底部内边距为 `12rpx + safe-area`，按钮卡片与 Home Indicator 之间留白过大。
- 最终改为基础底边距 `0`，刘海屏使用 `safe-area - 12rpx`；相较原实现共收紧 `24rpx`，同时仍保留按钮与 Home Indicator 的可见间隔。
- 字体、颜色、图片、文案、按钮高度和五个编辑模块均未变化；本轮无需额外聚焦字体或素材对比。
- 修复后未发现新的 P0、P1 或 P2 视觉问题，底部操作区无裁切、遮挡或溢出。

### Verification

- `npm run verify`：通过，包含 TypeScript 检查、5 项测试和 `git diff --check`。
- 微信开发者工具目标路由实际渲染，`Errors: 0`；现有 Skyline、旧 API 与 iconfont 警告不属于本次安全区改动。
- 未执行真机验收。

final result: passed
